# 本地版配置（复制为 .env.local 使用）
# 端口
PORT=3000

# 本地数据库文件路径（默认 data/db.json）
# LOCAL_DB_PATH=./data/db.json

# JWT 密钥（请修改为随机字符串）
LOCAL_JWT_SECRET=nanyong-todo-local-secret-change-me

# AI 解析默认配置（可选，用户在设置页可单独配置）
# AI_BASE_URL=https://api.openai.com/v1
# AI_API_KEY=sk-xxx
# AI_MODEL=gpt-4o-mini
